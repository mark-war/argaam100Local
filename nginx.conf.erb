server {
    listen 80;
    server_name  localhost;
  
    root /app/dist;
  
    location / {
      try_files $uri $uri/ /index.html;
    }
  
    location /assets/ {
      expires 1y;
      add_header Cache-Control public;
    }
  }
  